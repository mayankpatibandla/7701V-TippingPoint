#pragma once

#include "vex.h"

using namespace vex;

enum turnDirection
{
  Left, Right, Auto
};

enum e_teamColor
{
  Blue, Red
};

enum e_teamSide
{
  sideRight, sideLeft
};

enum odomType
{
  Classic, Inertial
};

enum angleUnits
{
  Rad, Deg
};